<div class="box-header with-border">
        <a href="{{ path('alumnus_ajouter') }}" class="pull-right btn btn-primary">{{ 'rh.alumni_ajouter'|trans({}, 'rh') }}</a>
</div>

<table class="display dataTable table table-bordered table-striped dt-responsive "> 
        <thead>
                <tr>
                        <th>{{ 'rh.nom'|trans({}, 'rh') }}</th> 
			<th>{{ 'rh.promotion'|trans({}, 'rh') }}</th>
			<th>{{ 'rh.ancien_poste'|trans({}, 'rh') }}</th>
			<th>{{ 'rh.mail'|trans({}, 'rh') }}</th>
			<th>{{ 'rh.tel'|trans({}, 'rh') }}</th>
			<th>{{ 'rh.lien_linkedin'|trans({}, 'rh') }}</th>
                        <th>{{ 'rh.poste'|trans({}, 'rh') }}</th>
                        <th>{{ 'rh.dernier_contact'|trans({}, 'rh') }}</th>
                        <th>{{ 'rh.commentaires'|trans({}, 'rh') }}</th>
                </tr>
        </thead>
	<tbody>
                {% for alumnus in alumni %}  
			<tr>
				<td>
					{# <a href="{{ path('alumni_contact_modifier', {'id': contact.id}) }}">{{ 'rh.contact'|trans({}, 'rh') }}
						n°{{loop.index0}}</a> #}
                                                {{alumnus.personne}}
				</td>
				<td>
						{{alumnus.commentaire}}
				</td>	
			</tr>
		{% endfor %}
	</tbody>
</table>


